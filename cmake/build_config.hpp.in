#pragma once

/// \file
/// Generated by CMake from cmake/build_config.hpp.in — do not edit by hand.
///
/// Everything here is a build-time fact captured at configure time. Runtime
/// facts (such as the OpenSSL version actually loaded) are queried at runtime
/// in src/version.cpp instead, because the two can differ.

// clang-format off

#define AMARIAN_VERSION_MAJOR      @PROJECT_VERSION_MAJOR@
#define AMARIAN_VERSION_MINOR      @PROJECT_VERSION_MINOR@
#define AMARIAN_VERSION_PATCH      @PROJECT_VERSION_PATCH@
#define AMARIAN_VERSION_STRING     "@PROJECT_VERSION@"

/// Pre-release qualifier. Empty on a tagged release.
#define AMARIAN_VERSION_PRERELEASE "@AMARIAN_VERSION_PRERELEASE@"

/// Short git commit, or "unknown" when built outside a git checkout
/// (for example from a source tarball).
#define AMARIAN_GIT_COMMIT         "@AMARIAN_GIT_COMMIT@"
#define AMARIAN_GIT_DIRTY          @AMARIAN_GIT_DIRTY@

#define AMARIAN_COMPILER_ID        "@CMAKE_CXX_COMPILER_ID@"
#define AMARIAN_COMPILER_VERSION   "@CMAKE_CXX_COMPILER_VERSION@"
#define AMARIAN_BUILD_TYPE         "@CMAKE_BUILD_TYPE@"
#define AMARIAN_TARGET_SYSTEM      "@CMAKE_SYSTEM_NAME@"
#define AMARIAN_TARGET_PROCESSOR   "@CMAKE_SYSTEM_PROCESSOR@"

/// Active sanitizer selection, empty when none.
#define AMARIAN_SANITIZER_STRING   "@AMARIAN_SANITIZER@"
#define AMARIAN_HARDENING_ENABLED  @AMARIAN_HARDENING_01@

/// Dependency versions as resolved at configure time.
#define AMARIAN_SECP256K1_VERSION  "@AMARIAN_SECP256K1_VERSION@"
#define AMARIAN_OPENSSL_VERSION    "@OPENSSL_VERSION@"

// clang-format on
