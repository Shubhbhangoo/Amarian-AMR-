const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=matchMedia('(pointer:fine)').matches;const progress=document.querySelector('.progress');const copy=document.getElementById('copy');const entry=document.getElementById('entry');const enter=document.getElementById('enter');const command='cmake --preset dev && cmake --build build/dev && ctest --preset dev';
const polish=document.createElement('link');polish.rel='stylesheet';polish.href='site-polish.css?v=20260910';document.head.appendChild(polish);
const immersive=document.createElement('link');immersive.rel='stylesheet';immersive.href='immersive.css?v=20260910';document.head.appendChild(immersive);
if(entry&&enter){const leave=()=>{entry.classList.add('is-entering');setTimeout(()=>entry.classList.add('is-entered'),850);sessionStorage.setItem('amarian-entered','1')};if(sessionStorage.getItem('amarian-entered')==='1'||reduced){entry.classList.add('is-entered')}else{enter.addEventListener('click',leave);enter.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' ')leave()})}}
addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;if(progress)progress.style.transform=`scaleX(${max?scrollY/max:0})`},{passive:true});
if(copy)copy.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(command);copy.textContent='COPIED';setTimeout(()=>copy.textContent='COPY',1400)}catch{copy.textContent='SELECT'}});
const reveals=document.querySelectorAll('.reveal');if(reduced||!('IntersectionObserver'in window))reveals.forEach(el=>el.classList.add('is-visible'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(el=>io.observe(el))}
const links=[...document.querySelectorAll('.site-header nav a')];const sections=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);if('IntersectionObserver'in window){const navIO=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${e.target.id}`))}),{rootMargin:'-35% 0px -55%'});sections.forEach(s=>navIO.observe(s))}
if(!reduced&&fine){document.documentElement.classList.add('motion-ready');const art=document.querySelector('.hero-art'),core=document.querySelector('.hero-core'),rings=[...document.querySelectorAll('.hero-ring')],notes=[...document.querySelectorAll('.hero-note')];let mx=.5,my=.5,rx=.5,ry=.5,raf=0;const move=e=>{mx=e.clientX/innerWidth;my=e.clientY/innerHeight;if(!raf)raf=requestAnimationFrame(frame)};const frame=()=>{rx+=(mx-rx)*.075;ry+=(my-ry)*.075;const px=(rx-.5)*2,py=(ry-.5)*2;if(art){art.style.setProperty('--mx',`${rx*100}%`);art.style.setProperty('--my',`${ry*100}%`)}if(core)core.style.transform=`translate3d(${px*24}px,${py*18}px,0) scale(${1+Math.abs(px+py)*.016})`;rings.forEach((r,i)=>{const d=i+1;r.style.transform=`translate3d(${px*d*9}px,${py*d*7}px,0) rotate(${[-18,63,0][i]}deg) scaleY(${[.46,.35,1][i]})`});notes.forEach((n,i)=>n.style.transform=`translate3d(${px*(i+1)*7}px,${py*(i+1)*5}px,0)`);raf=0};addEventListener('pointermove',move,{passive:true});addEventListener('pointerleave',()=>{mx=my=.5});const spotlight=document.createElement('span');spotlight.className='cursor-light';document.body.appendChild(spotlight);let lx=innerWidth/2,ly=innerHeight/2;addEventListener('pointermove',e=>{lx=e.clientX;ly=e.clientY;document.documentElement.style.setProperty('--cursor-x',`${lx}px`);document.documentElement.style.setProperty('--cursor-y',`${ly}px`);spotlight.style.left=`${lx}px`;spotlight.style.top=`${ly}px`},{passive:true});const magnetic=[...document.querySelectorAll('.button,.header-cta,.doc-links a')];magnetic.forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;el.style.setProperty('--mx',`${x*13}px`);el.style.setProperty('--my',`${y*9}px`);el.classList.add('pointer-hot')});el.addEventListener('pointerleave',()=>{el.style.setProperty('--mx','0px');el.style.setProperty('--my','0px');el.classList.remove('pointer-hot')})});const nodes=[...document.querySelectorAll('.arch-node')];nodes.forEach(node=>node.addEventListener('pointermove',e=>{const r=node.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;node.style.setProperty('--rx',`${-y*7}deg`);node.style.setProperty('--ry',`${x*8}deg`);node.style.setProperty('--mx',`${x*8}px`);node.style.setProperty('--my',`${y*8}px`);node.classList.add('pointer-hot')}));nodes.forEach(node=>node.addEventListener('pointerleave',()=>{node.style.setProperty('--rx','0deg');node.style.setProperty('--ry','0deg');node.style.setProperty('--mx','0px');node.style.setProperty('--my','0px');node.classList.remove('pointer-hot')}));const trail=[];for(let i=0;i<9;i++){const s=document.createElement('i');s.className='pointer-trail';document.body.appendChild(s);trail.push({el:s,x:innerWidth/2,y:innerHeight/2})}addEventListener('pointermove',e=>{let x=e.clientX,y=e.clientY;trail.forEach((p,i)=>{p.x+=(x-p.x)*(0.28-i*.018);p.y+=(y-p.y)*(0.28-i*.018);p.el.style.left=`${p.x}px`;p.el.style.top=`${p.y}px`;p.el.style.opacity=`${(9-i)/16}`})},{passive:true});const ripple=e=>{const r=document.createElement('span');r.className='click-ripple';r.style.left=`${e.clientX}px`;r.style.top=`${e.clientY}px`;document.body.appendChild(r);requestAnimationFrame(()=>r.classList.add('explode'));setTimeout(()=>r.remove(),650)};addEventListener('click',ripple,{passive:true});
const textSelectors='h1,h2,h3,p,.eyebrow,.section-kicker,.hero-note,.site-header nav a,.brand span,.site-footer span';const textEls=[...document.querySelectorAll(textSelectors)].filter(el=>el.children.length===0&&el.textContent.trim().length>1);textEls.forEach(el=>{const text=el.textContent;const frag=document.createDocumentFragment();const tokens=text.split(/(\s+)/);tokens.forEach(token=>{if(/^\s+$/.test(token)){frag.appendChild(document.createTextNode(token));return}if(!token)return;const word=document.createElement('span');word.className='magnetic-word';[...token].forEach(ch=>{const s=document.createElement('span');s.className='magnetic-char';s.textContent=ch;word.appendChild(s)});frag.appendChild(word)});el.textContent='';el.classList.add('magnetic-text');el.appendChild(frag)});const magneticTexts=textEls;let textX=-9999,textY=-9999;addEventListener('pointermove',e=>{textX=e.clientX;textY=e.clientY;magneticTexts.forEach(el=>{const r=el.getBoundingClientRect();el.classList.toggle('is-near',e.clientX>=r.left-90&&e.clientX<=r.right+90&&e.clientY>=r.top-65&&e.clientY<=r.bottom+65)})},{passive:true});const textFrame=()=>{magneticTexts.forEach(el=>{if(!el.classList.contains('is-near'))return;el.querySelectorAll('.magnetic-char').forEach(ch=>{const r=ch.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=cx-textX,dy=cy-textY,d=Math.hypot(dx,dy),p=Math.max(0,1-d/145),q=p*p;ch.style.setProperty('--tx',`${-dx*.075*q}px`);ch.style.setProperty('--ty',`${-dy*.12*q}px`);ch.style.setProperty('--tr',`${-dx*.018*q}deg`);ch.style.setProperty('--ts',`${1+q*.035}`);ch.style.setProperty('--tb',`${q*.35}px`)})});requestAnimationFrame(textFrame)};requestAnimationFrame(textFrame);magneticTexts.forEach(el=>el.addEventListener('pointerdown',e=>{const r=el.getBoundingClientRect(),ring=document.createElement('span');ring.className='magnetic-ripple';ring.style.left=`${e.clientX-r.left}px`;ring.style.top=`${e.clientY-r.top}px`;el.appendChild(ring);setTimeout(()=>ring.remove(),950)}))}

/* Premium immersive layer: ambient network field, instrument HUD and depth cues. */
(()=>{const hero=document.querySelector('.hero-art');if(!hero)return;const hud=document.createElement('div');hud.className='immersive-hud';hud.innerHTML='<i class="hud-corner tl"></i><i class="hud-corner tr"></i><i class="hud-corner bl"></i><i class="hud-corner br"></i><span class="hud-readout hud-top"><i class="hud-pulse"></i>NETWORK / CONSENSUS FIELD</span><span class="hud-readout hud-bottom">AMR · C++23 · POW / 300S</span><span class="hud-scroll">SCROLL TO DESCEND</span>';document.body.appendChild(hud);const atm=document.createElement('div');atm.className='network-atmosphere';const canvas=document.createElement('canvas');atm.appendChild(canvas);hero.prepend(atm);const scan=document.createElement('div');scan.className='network-scan';hero.appendChild(scan);const strip=document.createElement('div');strip.className='state-strip';strip.innerHTML='<i></i><span>CONSENSUS ENGINE</span><b>READY</b><span>·</span><span>STATE TRANSITIONS</span><b>DETERMINISTIC</b>';const copyBlock=document.querySelector('.hero-copy');if(copyBlock)copyBlock.appendChild(strip);const ctx=canvas.getContext('2d');const nodes=[];const count=window.innerWidth<700?22:42;const resize=()=>{const r=hero.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=Math.max(1,Math.floor(r.width*d));canvas.height=Math.max(1,Math.floor(r.height*d));ctx.setTransform(d,0,0,d,0,0);nodes.forEach(n=>{n.w=r.width;n.h=r.height})};for(let i=0;i<count;i++)nodes.push({x:Math.random(),y:Math.random(),vx:(Math.random()-.5)*.0007,vy:(Math.random()-.5)*.0007,r:Math.random()*1.5+.45,w:0,h:0,p:Math.random()*Math.PI*2});let last=0;const draw=t=>{if(reduced){ctx.clearRect(0,0,canvas.clientWidth,canvas.clientHeight);return}if(t-last<28){requestAnimationFrame(draw);return}last=t;const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);nodes.forEach(n=>{n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>1)n.vx*=-1;if(n.y<0||n.y>1)n.vy*=-1});for(let i=0;i<nodes.length;i++){const a=nodes[i],ax=a.x*w,ay=a.y*h;for(let j=i+1;j<nodes.length;j++){const b=nodes[j],bx=b.x*w,by=b.y*h,dx=ax-bx,dy=ay-by,dist=Math.hypot(dx,dy);if(dist<120){ctx.strokeStyle=`rgba(80,90,79,${(1-dist/120)*.12})`;ctx.lineWidth=.6;ctx.beginPath();ctx.moveTo(ax,ay);ctx.lineTo(bx,by);ctx.stroke()}}ctx.fillStyle=`rgba(78,89,77,${.22+.12*Math.sin(t*.0015+a.p)})`;ctx.beginPath();ctx.arc(ax,ay,a.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)};resize();addEventListener('resize',resize,{passive:true});requestAnimationFrame(draw);hero.classList.add('is-network-active');const depth=[...document.querySelectorAll('.section-block,.asset-layout,.verify-layout,.build-layout')];depth.forEach((el,i)=>el.setAttribute('data-section',`0${i+1} / AMARIAN`));if(!reduced){let sy=scrollY,sr=0;addEventListener('scroll',()=>{sy=scrollY;if(!sr)sr=requestAnimationFrame(()=>{depth.forEach(el=>{const r=el.getBoundingClientRect(),d=(innerHeight/2-(r.top+r.height/2))/innerHeight;el.style.setProperty('--depth-y',`${Math.max(-8,Math.min(8,d*5))}px`)});sr=0})},{passive:true})}})();
