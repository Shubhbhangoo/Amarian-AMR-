const reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;
const copy=document.getElementById('copy');
const command='cmake --preset dev && cmake --build build/dev && ctest --preset dev';
if(copy)copy.onclick=async()=>{try{await navigator.clipboard.writeText(command);copy.textContent='COPIED';setTimeout(()=>copy.textContent='COPY',1400)}catch{copy.textContent='SELECT'}};
const hero=document.querySelector('.hero'),object=document.querySelector('.hero-object'),diamond=document.querySelector('.diamond-object');
if(hero&&object&&!reduceMotion&&matchMedia('(pointer:fine)').matches){let tx=0,ty=0,x=0,y=0;hero.onpointermove=e=>{const r=hero.getBoundingClientRect();tx=(e.clientX-r.left)/r.width-.5;ty=(e.clientY-r.top)/r.height-.5};hero.onpointerleave=()=>{tx=ty=0};const loop=()=>{x+=(tx-x)*.04;y+=(ty-y)*.04;object.style.transform=`translate(${x*24}px,${y*18}px)`;if(diamond)diamond.style.boxShadow=`${x*18}px ${y*18}px 110px #7b8f7e2b,inset 0 0 60px #fff`;requestAnimationFrame(loop)};loop()}
const revealEls=document.querySelectorAll('.chapter,.statement,.cap,.network-visual,.object-section,.node-section,.build-inner,.verify');
const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');reveal.unobserve(e.target)}}),{threshold:.08});revealEls.forEach(e=>reveal.observe(e));
const navLinks=[...document.querySelectorAll('.top nav a')],sections=[...document.querySelectorAll('main section[id]')];const navObs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)navLinks.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${e.target.id}`))}),{rootMargin:'-45% 0px -48% 0px'});sections.forEach(s=>navObs.observe(s));
if(!reduceMotion){document.querySelectorAll('.cap,.doc,.node').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',`${e.clientX-r.left}px`);el.style.setProperty('--my',`${e.clientY-r.top}px`)});el.addEventListener('pointerleave',()=>{el.style.removeProperty('--mx');el.style.removeProperty('--my')})})}
window.addEventListener('scroll',()=>{const y=scrollY;document.documentElement.style.setProperty('--scroll',y);const heroObject=document.querySelector('.hero-object');if(heroObject&&!reduceMotion&&y<innerHeight*1.1)heroObject.style.marginTop=`${y*.08}px`},{passive:true});
